
<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>login</title>
    <style>
      .login-form {
  			width: 300px;
  			margin: 0 auto;
  			font-family: Arial, sans-serif;
  		}
  		.login-form h1 {
  			text-align: center;
  			color: #4d4d4d;
  			font-size: 24px;
  			padding: 20px 0 20px 0;
  		}
  		.login-form input {
  			width: 100%;
  			padding: 15px;
  			border: 1px solid #dddddd;
  			margin-bottom: 15px;
  			box-sizing:border-box;
        font-size: 13px;
  		}
      .login-form input:focus {
        outline: none;
        border: 1px solid #9bc8ff;
      }
  		.login-form button {
  			width: 100%;
  			padding: 15px;
  			background-color: #535b63;
  			border: 0;
        outline: none;
  			box-sizing: border-box;
  			cursor: pointer;
  			font-weight: bold;
        font-size: 13px;
  			color: #ffffff;
  		}
      .login-form p {
        color: #4d4d4d;
        font-size: 12px;
      }
      .login-form a:link,
      .login-form a:visited,
      .login-form a:link,
      .login-form a:active {
        color: #9bc8ff;
        text-decoration: none;
      }
      .login-form a:hover {
        color: #9bc8ff;
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="login-form">
      <h1>Login</h1>
      <form action="/login" method="POST">
        <p>EMAIL
          <% if (messages.error == 'Email incorrect') { %>
            <%= ' - Email does not exist.' %>
          <% } %></p>
        <input type="email" id="email" name="email" required  >
        <p>PASSWORD
          <% if (messages.error == 'Password incorrect') { %>
            <%= ' - Passoword does not match.' %>
          <% } %></p>
        <input type="password" id="password" name="password" required>
        <button onclick="require()">Login</button>
        <p>Need an account? <a href='/register'>Register</a></p>
      </form>
    </div>
  </body>
</html>
